<UserControl x:Class="configControlWpf.SearchTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:configControlWpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="textBox"/>
            <Button x:Name="button" Content="Get Html Text" Grid.Column="1"/>
        </Grid>
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="..." Grid.Row="1"/>
        <TabControl x:Name="tabControl" Grid.Row="2">
            <TabItem x:Name="tiTest" Header="Test">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="btnTest" Content="Test"/>
                    </StackPanel>
                    <TextBox x:Name="txtTest" TextWrapping="Wrap" Text="TextBox" Grid.Row="1"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tiHtmlText" Header="Html Text">
                <TextBox x:Name="txtHtmlText" TextWrapping="Wrap" Text="TextBox"/>
            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
